# =============================================================================
# Cấu hình tập trung — TOÀN BỘ giá trị khai-tay của service (nhóm B).
#
# - KHÔNG chứa secret. Secret (API key/token) CHỈ đọc từ env — xem .env.example.
# - Đọc qua src/config.py (load 1 lần, lru_cache) — KHÔNG yaml.load file này
#   ở chỗ khác.
# - Đường dẫn là relative so với repo root.
# - Thuật toán/công thức (nhóm C) vẫn nằm trong code; đây chỉ là tham số.
# =============================================================================

paths:
  data_xlsx: data/ai_maps_track2_dataset_participants.xlsx
  attribute_concepts: lexicon/attribute_concepts.yaml
  gazetteer: lexicon/gazetteer.yaml
  categories: lexicon/categories.yaml
  abbreviations: config/abbreviations.yaml
  city_aliases: config/city_aliases.yaml
  embedding_cache_dir: data/cache      # .npy cache — gitignore, xoá là tự build lại
  qdrant_storage_dir: data/qdrant_storage  # Qdrant local mode (embedded) — gitignore, rebuild từ .npy
  reports_dir: eval/reports
  readme_md: README.md

# Tên sheet trong xlsx (đã verify bằng eval/verify_dataset.py)
sheets:
  poi: POI_Dataset
  eval: Public_Evaluation

embedding:
  # e5-small đã đo retrieval-only 0.950 trên corpus 111 doc. TODO thử e5-base/bge-m3
  # như ablation NHƯNG phải đo lại — corpus nhỏ, không mặc định model to hơn là tốt hơn.
  model: intfloat/multilingual-e5-small

eval:
  top_k: 10          # số kết quả retriever trả cho eval (MRR tính trong top-k)

search:
  default_limit: 10  # limit mặc định của SearchService.search
  k_internal: 50     # k nội bộ > limit để post-filter (category/radius/bbox) không đói kết quả
  api_max_limit: 20  # "max recommended 20" theo docs/tasco_api.pdf
  version_poll_seconds: 10  # chu kỳ poll data_version (Phase 5, chỉ nguồn postgres)

retrieval:
  n_candidates: 30   # đủ sâu: 60 câu eval đều có đáp án trong top-30 BM25 (recall@30 ~1.0)
  pool_k: 25         # union pool: top-25 BM25 ∪ top-25 dense — tối đa recall cho reranker
  # Cosine threshold cho dynamic attribute matching (AttributeIndex.radius_search):
  # query span vs unique POI attributes; ≥ ngưỡng → match (tối đa top-2).
  attribute_similarity_threshold: 0.85

# Trọng số 7-signal theo cấu hình pipeline.
# ⚠ GIỮ NGUYÊN THỨ TỰ KEY: total = tổng float cộng theo thứ tự này — đổi thứ tự
# có thể lệch bit cuối và đảo tie-break.
rerank_weights:
  # Bước 1 — chưa distance. bm25 = điểm base đã chuẩn hóa theo max trong candidate
  # set; phần còn lại là signal từ QueryPlan + POI. price_check/rating_check/
  # time_check/open_now_check: numeric constraint routing (dynamic-vector-attributes).
  default:
    bm25: 0.30
    category: 0.24
    attr: 0.20
    city: 0.10
    rating: 0.05
    pop: 0.03
    price_check: 0.03
    rating_check: 0.03
    time_check: 0.02
    open_now_check: 0.03
  # Bước 2: thêm distance (landmark/district), các weight khác giảm nhẹ tương ứng.
  with_distance:
    bm25: 0.26
    category: 0.20
    attr: 0.18
    city: 0.09
    distance: 0.13
    rating: 0.03
    pop: 0.02
    price_check: 0.03
    rating_check: 0.03
    time_check: 0.03
    open_now_check: 0.03
  # Slice dense-as-signal: dense_relevance là relevance CHÍNH; BM25 hạ mạnh (thủ phạm
  # dính bait G ở P036/P042) + tách "name" = khớp chính xác tên/brand. Các weight
  # structured (category/attr/city/distance/rating/pop) GIỮ NGUYÊN từ v2 — chúng đang
  # bảo vệ mixed-lang/location queries, đừng đụng.
  with_dense:
    dense: 0.28
    bm25: 0.05
    name: 0.05
    category: 0.20
    attr: 0.18
    city: 0.08
    distance: 0.12
    rating: 0.02
    pop: 0.02
    price_check: 0.03
    rating_check: 0.03
    time_check: 0.03
    open_now_check: 0.03

understanding:
  # Cửa sổ (số ký tự) trước landmark phải chứa 1 cue trong landmark_near_cues
  # thì mới resolve — bẫy P009 "trên đường đi hạ long" không có cue → bỏ qua.
  landmark_near_cue_window: 15
  # Wordlist trigger (dạng ĐÃ BỎ DẤU — match trên text normalize). Cấu trúc
  # ngữ pháp của luật (word-boundary, "không (quá)...") vẫn nằm trong rules.py;
  # đây chỉ là danh sách từ để non-dev bổ sung không phải sửa code.
  landmark_near_cues: [gan, near, canh, sat, quanh]
  popularity_cues: [noi tieng, famous, best, ngon, hot, dang di, dang den]
  # "không (quá) <crowd_terms> (khách/đúc/người/ào)?" → yên tĩnh + phủ định đông
  # ⚠ "on" phải QUOTE — YAML 1.1 parse on/off/yes/no trần thành boolean
  crowd_terms: [dong, "on"]
  quiet_phrases: [vang ve, it nguoi, it dong]
  diacritics:
    max_gram: 4        # greedy longest-match phrase 4→2 gram
    min_coverage: 0.5  # tỉ lệ token phục-hồi-được dưới ngưỡng → coi là câu ngoại ngữ, không restore
  typo:
    min_token_len: 3       # token ngắn hơn → không đụng (quá dễ va chạm)
    junk_strip_min_len: 4  # strip ký tự rác cuối chỉ khi token ≥ 4 (stripped còn ≥ 3)
    trailing_junk_chars: fjzw  # chữ không kết thúc âm tiết thuần Việt (Telex thiếu IME);
                               # cố ý KHÔNG gồm s/r/x (hotels, bar, box...)

constraints:
  satisfied_threshold: 0.5   # score ≥ ngưỡng → "thỏa"; dưới → liệt kê vào "nới"
  # Keyword (ĐÃ BỎ DẤU, substring match trên normalize_vi(attribute string)) nâng
  # attribute thành constraint CÓ KIỂU riêng (chấm bằng field cấu trúc).
  # v2 dynamic-vector-attributes: plan mang RAW attribute string, không còn concept id.
  time_keywords: [mo khuya, 24/7, mo muon, hai bon bay]
  price_keywords: [gia re, mien phi]
  price_max_level:           # price_level trong data: 1..4 (key = keyword, substring)
    gia re: 2
    mien phi: 1
  # "mở khuya": đóng ≥ 23:00 → thỏa 1.0; ≥ 22:00 → thỏa một phần
  late_close_full_minutes: 1380    # 23:00
  late_close_partial_minutes: 1320 # 22:00
  time_partial_score: 0.5          # điểm khi chỉ đạt mốc partial (22:00–23:00)
  # location theo landmark coord: ≤ near_km_full → 1.0; ≤ near_km_partial → partial
  near_km_full: 1.0
  near_km_partial: 3.0
  location_partial_score: 0.7      # trong bán kính partial
  location_same_city_score: 0.2    # ngoài bán kính nhưng đúng city (theo coord)
  location_city_fallback_score: 0.5  # constraint district: khác quận nhưng đúng city

# Ingestion endpoint (POST /admin/pois/batch)
ingestion:
  max_batch_size: 500  # vượt → 413 ngay, KHÔNG gọi AWS/DB (chống runaway cost + timeout)

# Phase 4b — verify POI ingest bằng AWS Location Geocode v2 (geo-places).
# Credential KHÔNG ở đây — boto3 default chain (env/~/.aws); đây chỉ là tham số.
verify:
  aws_region: ap-southeast-2  # PHẢI cùng region với API key (key là tài nguyên theo region)
  # Ngưỡng tune theo smoke 39 POI thật (scripts/smoke_geocode.py, 2026-07-11):
  # score tách 2 cụm rõ (0.73–0.83 vs 0.98–0.99, không vùng xám), cụm thấp có
  # tính hệ thống theo vùng (toàn TP.HCM/Đà Lạt — match đúng khu vực, chỉ khác
  # văn phong địa chỉ) → 0.70 để không phạt oan.
  match_score_threshold: 0.70  # MatchScores.Overall ≥ ngưỡng mới tính verified
  # 500m (nới từ 150m, quyết định 2026-07-12): toạ độ dataset mock lệch có hệ
  # thống ~200-400m ngay cả khi AWS tìm ra ĐÚNG doanh nghiệp bằng tên (Pizza
  # 4P's: pin lệch 336m khỏi quán thật) — 150m phạt gần như toàn bộ data mock.
  # 500m vẫn bắt được pin sai hẳn khu (>0.5km: Bà Nà 13.6km, Ocean View 4.5km).
  max_distance_m: 500          # toạ độ claim vs matched lệch quá → unverified
  place_types: [PointAddress, PointOfInterest, Street]  # Street: địa danh không số nhà (vd A002)
  max_workers: 10              # verify song song có giới hạn (dưới rate limit AWS)
  throttle_retry_delays: [1, 2, 4]  # backoff (giây) khi ThrottlingException
  http_timeout_seconds: 10     # timeout mỗi HTTP call tới Geocode v2

# Feature flags — default ở đây; env override từng cái (xem .env.example)
features:
  typo_fix: true  # env TASCO_TYPO_FIX=0/1
